# Reddit Cabinet

Веб-приложение для управления и отслеживания статистики аккаунтов Reddit.

## Функционал

- 🔐 Регистрация и вход по email и паролю
- 📊 Добавление аккаунтов Reddit
- 📈 Получение статистики по аккаунтам:
  - Количество комментариев
  - Карма
  - Возраст аккаунта (в днях)
  - Количество постов
  - Количество подписчиков

## Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - Типизация
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL** - База данных
- **Tailwind CSS** - Стилизация
- **JWT** - Аутентификация
- **bcrypt** - Хеширование паролей

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Настройте переменные окружения:
Создайте файл `.env` в корне проекта:
```
DATABASE_URL="postgresql://user:password@localhost:5432/reddit_cabinet"
JWT_SECRET="your-secret-key-change-in-production"
```

3. Настройте базу данных:
```bash
npx prisma generate
npx prisma migrate dev --name init
```

4. Запустите проект:
```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

## 🚀 Деплой на Vercel

### Подготовка к деплою

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/reddit-cabinet.git
   git push -u origin main
   ```

2. **Подключите проект к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com/login)
   - Нажмите "Add New Project"
   - Импортируйте ваш GitHub репозиторий
   - Vercel автоматически определит Next.js

### Настройка базы данных для Vercel

**⚠️ Важно:** Vercel не поддерживает SQLite. Используйте PostgreSQL:

**Вариант 1: Vercel Postgres (рекомендуется)**
- В настройках проекта Vercel перейдите в "Storage"
- Создайте новую базу данных "Postgres"
- Vercel автоматически добавит переменную `POSTGRES_PRISMA_URL` и `POSTGRES_URL_NON_POOLING`

**Вариант 2: Neon (бесплатный PostgreSQL)**
- Зарегистрируйтесь на [neon.tech](https://neon.tech)
- Создайте новый проект
- Скопируйте Connection String
- Используйте его как `DATABASE_URL`

**Вариант 3: Supabase**
- Зарегистрируйтесь на [supabase.com](https://supabase.com)
- Создайте новый проект
- Перейдите в Settings → Database
- Скопируйте Connection String

### Настройка переменных окружения в Vercel

В настройках проекта Vercel → Settings → Environment Variables добавьте:

**Обязательные:**
```
DATABASE_URL="postgresql://user:password@host:5432/database"
JWT_SECRET="your-random-secret-key-min-32-chars"
```

**Опциональные (для полного функционала):**
```
MEGA_EMAIL="your-email@example.com"
MEGA_PASSWORD="your-password"
MEGA_RECOVERY_KEY="your-recovery-key"

SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
SMTP_FROM="noreply@yourdomain.com"

NEXT_PUBLIC_APP_URL="https://your-domain.vercel.app"
```

### Миграции базы данных

После первого деплоя выполните миграции:
```bash
# Локально (если у вас есть доступ к базе)
npx prisma migrate deploy

# Или через Vercel CLI
vercel env pull .env.local
npx prisma migrate deploy
```

Или добавьте в `package.json` скрипт для автоматического выполнения миграций:
```json
"scripts": {
  "postinstall": "prisma generate && prisma migrate deploy"
}
```

### Проверка деплоя

После деплоя проверьте:
1. ✅ Приложение открывается по адресу `https://your-project.vercel.app`
2. ✅ Регистрация и вход работают
3. ✅ База данных подключена (проверьте логи в Vercel Dashboard)
4. ✅ API routes работают

### Обновление схемы базы данных

При изменении `prisma/schema.prisma`:
1. Создайте миграцию локально: `npx prisma migrate dev --name migration-name`
2. Закоммитьте изменения: `git add . && git commit -m "Update schema"`
3. Запушьте в GitHub: `git push`
4. Vercel автоматически выполнит `prisma migrate deploy` при деплое

## Структура проекта

```
├── app/
│   ├── api/          # API routes
│   ├── dashboard/    # Страница кабинета
│   ├── login/        # Страница входа
│   ├── register/     # Страница регистрации
│   └── layout.tsx    # Главный layout
├── lib/              # Утилиты и хелперы
├── prisma/           # Схема базы данных
└── public/           # Статические файлы
```

## Примечания

- Reddit API имеет ограничения на количество запросов
- Для получения приватных данных требуется OAuth авторизация Reddit
- Пароли Reddit аккаунтов хранятся в открытом виде (рекомендуется добавить шифрование)











